name: Test

on:
  push:
    branches: [ "main" ]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/test.yml'

jobs:
  # Unit Tests
  unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run unit tests
      run: go test ./... -v -short

  # SQLite Tests - Normal Mode
  sqlite-host-normal-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (host style, no replication, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style host -repl-mode none -blob-store sql

  sqlite-host-normal-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (host style, no replication, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style host -repl-mode none -blob-store filesystem

  sqlite-path-normal-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (path style, no replication, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style path -repl-mode none -blob-store sql

  sqlite-path-normal-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (path style, no replication, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style path -repl-mode none -blob-store filesystem

  # SQLite Tests - Replicated Mode
  sqlite-host-replicated-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (host style, replicated, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style host -repl-mode replicated -blob-store sql

  sqlite-host-replicated-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (host style, replicated, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style host -repl-mode replicated -blob-store filesystem

  sqlite-path-replicated-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (path style, replicated, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style path -repl-mode replicated -blob-store sql

  sqlite-path-replicated-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run SQLite integration tests (path style, replicated, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db sqlite -path-style path -repl-mode replicated -blob-store filesystem

  # PostgreSQL Tests - Normal Mode
  postgres-host-normal-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (host style, no replication, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style host -repl-mode none -blob-store sql

  postgres-host-normal-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (host style, no replication, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style host -repl-mode none -blob-store filesystem

  postgres-path-normal-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (path style, no replication, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style path -repl-mode none -blob-store sql

  postgres-path-normal-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (path style, no replication, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style path -repl-mode none -blob-store filesystem

  # PostgreSQL Tests - Replicated Mode
  postgres-host-replicated-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (host style, replicated, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style host -repl-mode replicated -blob-store sql

  postgres-host-replicated-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (host style, replicated, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style host -repl-mode replicated -blob-store filesystem

  postgres-path-replicated-sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (path style, replicated, SQL blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style path -repl-mode replicated -blob-store sql

  postgres-path-replicated-fs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Run PostgreSQL integration tests (path style, replicated, filesystem blob store)
      run: go test ./... -v -timeout 4h -integration -db postgres -path-style path -repl-mode replicated -blob-store filesystem
